# HUD & 상태 아이콘 (21종)

> FLUX 1.1 Pro용. aspect_ratio: 1:1, output_format: png
> guidance: 7.5, steps: 50

## 공통 프리픽스

```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size,
```

## 공통 서픽스

```
, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```

---

## 생명 아이콘 (3종)

### hud_heart_full — 풀 하트
**Seed:** 78001
```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size, full red heart, solid bright crimson red pixel heart shape, classic game health icon, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```

### hud_heart_half — 반 하트
**Seed:** 78002
```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size, half red heart, left half bright crimson right half dark gray empty, half health indicator, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```

### hud_heart_empty — 빈 하트
**Seed:** 78003
```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size, empty heart outline, dark gray hollow heart shape outline only, no fill, empty health slot, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```

---

## 카드 관련 HUD (4종)

### hud_card_back — 카드 뒤 표시
**Seed:** 78011
```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size, tiny card back icon, dark blue-purple rectangle with small pattern, remaining deck indicator, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```

### hud_card_special — 특수카드 마커
**Seed:** 78012
```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size, golden star on small card shape, special card indicator, bright gold star marker, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```

### hud_card_match — 매칭 하이라이트
**Seed:** 78013
```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size, two small overlapping card shapes with bright connection glow between them, match indicator, golden connecting light, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```

### hud_card_discard — 버리기 표시
**Seed:** 78014
```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size, card shape with red X mark over it, discard indicator, rejected card, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```

---

## 진행 아이콘 (4종)

### hud_gate_number — 관문 번호 프레임
**Seed:** 78021
```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size, small Korean gate arch shape frame, dark stone gate indicator, number display frame, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```

### hud_round_counter — 판 카운터
**Seed:** 78022
```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size, small circular counter frame, dark ring with bright center, round number display, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```

### hud_turn_indicator — 턴 표시
**Seed:** 78023
```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size, small arrow pointing right, bright golden turn arrow, current turn indicator, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```

### hud_phase_marker — 페이즈 마커
**Seed:** 78024
```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size, small diamond shape phase indicator, bright phase marker, current game phase, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```

---

## 탐욕 게이지

### hud_greed_gauge — 탐욕 게이지 프레임
**Seed:** 78031
```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size, vertical bar gauge frame, dark ornate frame with golden tick marks, greed meter empty state, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```

---

## 부적 슬롯 프레임 (3종)

### hud_talisman_empty — 빈 부적 슬롯
**Seed:** 78041
```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size, empty square slot frame, dark border with subtle talisman pattern, empty equipment slot, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```

### hud_talisman_filled — 장착된 부적 슬롯
**Seed:** 78042
```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size, square slot frame with golden glow inside, active talisman equipped indicator, bright golden fill, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```

### hud_talisman_locked — 잠긴 부적 슬롯
**Seed:** 78043
```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size, square slot frame with padlock overlay, locked talisman slot, dark gray locked state, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```

---

## 동료 슬롯 프레임 (2종)

### hud_companion_empty — 빈 동료 슬롯
**Seed:** 78051
```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size, circular empty companion slot frame, dark ring border with dokkaebi horn decoration, empty ally slot, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```

### hud_companion_active — 활성 동료 슬롯
**Seed:** 78052
```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size, circular companion slot frame with bright glow, active companion indicator, golden glow inside circle, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```

---

## 기타 HUD

### hud_gimmick_warning — 기믹 경고
**Seed:** 78061
```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size, warning triangle with exclamation mark, red alert danger indicator, boss gimmick incoming warning, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```

### hud_damage_frame — 데미지 숫자 프레임
**Seed:** 78071
```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size, small explosion burst shape frame for damage numbers, red impact burst, number display backing, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```

### hud_multiplier_frame — 배수 표시 프레임
**Seed:** 78072
```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size, small golden star burst frame for multiplier display, bright gold accent frame, multiplier indicator backing, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```

### hud_chip_frame — 점수 칩 프레임
**Seed:** 78073
```
16-bit pixel art game HUD icon, clean pixel grid, no anti-aliasing, limited color palette max 6 colors, single element centered, simple clear readable at small size, small circular chip frame for score display, dark blue-purple chip shape, Balatro-inspired score chip, solid pure green chroma key background, bold saturated colors, sharp pixels, no text, no watermark, no ground, no shadow
```
